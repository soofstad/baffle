services:
  baffle:
    image: baffle
    env_file:
      - .env
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
  backend:
    image: nginx
    ports:
      - "80:80"


